import Lottie from "lottie-react";
import pigCoinsAnimation from "../../styles/pig-coins.json";

const PigCoins = () => <Lottie animationData={pigCoinsAnimation} loop={true} />;

export default PigCoins;
